import { names, numbers } from "./data.js";
import { allBut, capitalize, doublle, first, firstOF, last, prove, randomBetween, sum, uppercase } from "./function.js";

firstOF(names, numbers)

allBut(names, numbers)

last(names, numbers)

first(names, numbers)

doublle(numbers)

sum(numbers)

randomBetween(1,5)

capitalize("sword")

uppercase("sword")

prove("peter","pep")

